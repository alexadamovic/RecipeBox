@{
  Layout = "_Layout";
}

@model RecipeBox.Models.Tag

<h2>Tags</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.TagName): @Html.DisplayFor(model => model.TagName)</h3>

@if(@Model.JoinEntities.Count == 0)
{
  <p>This tag is not associated with any recipes</p>
}
else
{
  <h4>Recipes the tag is associated with:</h4>
  <ul>
  @foreach(var join in Model.JoinEntities)
  {
    <li>@join.Recipe.RecipeName</li>
  }
  </ul>
}


<p>@Html.ActionLink("Edit Category", "Edit", new { id = Model.TagId })</p>
<p>@Html.ActionLink("Delete Category", "Delete", new { id = Model.TagId })</p>